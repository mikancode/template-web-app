# CLAUDE.md

<!-- 複製後に書き換える: このファイルはテンプレートの雛形。各節を実際のリポジトリの事実で置き換える -->

このリポジトリで作業するエージェントが、即座に実行・参照すべき事実だけを書く。
汎用の開発ルール（コミット形式・開発フロー・コードの書き方）は `~/.claude/CLAUDE.md`
が持つため、ここには書かない。

## テストの実行方法

<!-- 複製後に書き換える: 実際に通るコマンドだけを書く。存在しないコマンドを書くと検証が静かに失敗する -->

- テスト: `npm test`
- 型チェック: `npm run typecheck`
- lint: `npm run lint`

## 触ってはいけないディレクトリ

<!-- 複製後に書き換える: 自動生成物・外部から取り込んだコードなど、手で編集すると失われる場所 -->

- `node_modules/` — 依存の実体。変更は `package.json` 経由で行う
- `dist/` — ビルド成果物。ソースを直す

## コミット規約（.gitmessage 参照）

コミットテンプレートは `dotfiles/home/.gitmessage` をグローバル設定した `commit.template`
が供給する（`install.sh` がマシン単位で設定する）。このリポジトリ側に実体は置かない。

- タイトルは `<type>: <概要>` の Conventional Commits 形式
- トレーラーに `Refs: #<Issue番号>` を含める
